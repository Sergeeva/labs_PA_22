#include "algorithms/variants.h"

using namespace std;

int main() {
    int iterations = 1;
    int countOfThreads = 1;
    secondVariant(countOfThreads, iterations);
    // firstVariant(countOfThreads, iterations);
//   MatrixPair matrixes = generateMatrixPair();
//   Matrix result(N, vector<int>(N, 0));
//    subtract(matrixes.first, matrixes.second, result);

    return 0;
}